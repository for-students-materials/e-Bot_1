

// your code


module.exports = {

    /**
     * @param {*} message.format - you'r messages format text | audio 
     * @param {*} message.data - you'r messages text 
     * @returns message of other type
     */
    convert: async (message) => {
        // your code
    },

    
}